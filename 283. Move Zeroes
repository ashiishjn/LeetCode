class Solution 
{
    public void moveZeroes(int[] nums) 
    {
        int n=nums.length;
        int i,k=0;
        for(i=0;i<n;i++)
        {
            if(nums[i]!=0)
                nums[k++]=nums[i];
        }
        for(;k<n;k++)
            nums[k]=0;
    }
}
